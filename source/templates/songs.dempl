dempl Songs(models.Song[] songs, string code) {
    <div class="container mx-auto">
        <div class="flex flex-col space-y-2">
            foreach (song; songs) {
                <div>{ song.title } by { song.artist }</div>
            }
        </div>
        @Code(code)
    </div>
}